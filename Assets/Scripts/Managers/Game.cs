using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[System.Serializable]
public class Game {
    
    //public Vector3 Spawn { get => spawn; set => spawn = value; }

    public Game(PlayerBase player) {
        //Spawn = GameController.GetGameController().Spawn.transform.position;
        //ItemSlots with data in them. for each itemslot in all the different invents, gonna have to save the ones with data in them and then the quantities
        //The date int 
        //The time int

        
        Debug.Log("Game was saved");
    }
}
